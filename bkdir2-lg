echo "I am going to back up this directory to $1"
ssh -T $1 <<EOI
echo "I have sucessfully logged in $1"
if [ "$(pwd | grep ${wdir})" != "" ]
then
    cd ~/work${PWD#$wdir}
    dldir4 $localserver $2 $3 $4
else
    echo error: you are not in work directory
fi


